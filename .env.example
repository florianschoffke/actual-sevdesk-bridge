# SevDesk API credentials
SEVDESK_API_KEY=your_sevdesk_api_key_here

# Actual Budget configuration
ACTUAL_BUDGET_URL=http://your-actual-budget-server:5006
ACTUAL_BUDGET_PASSWORD=your_actual_budget_password
ACTUAL_BUDGET_FILE_ID=My Budget
ACTUAL_BUDGET_VERIFY_SSL=true

# Sync configuration
ACTUAL_ACCOUNT_NAME=EGB Funds

# Include voucher info in transaction notes (default: false)
# Set to true to include "Voucher: 202101 | ID: 122853317" in notes
# Set to false for cleaner transactions (deduplication uses imported_id, not notes)
INCLUDE_TRANSACTION_NOTES=false

# Sync Schedule (cron format: "minute hour day month day_of_week")  
# Examples:
#   "0 18 * * 2"  = Every Tuesday at 6:00 PM
#   "0 * * * *"   = Every hour (default)
#   "30 14 * * 1" = Every Monday at 2:30 PM
#   "0 9,17 * * 1-5" = Every weekday at 9 AM and 5 PM
SYNC_SCHEDULE=0 * * * *

LOG_LEVEL=INFO

# Email notifications for validation failures (optional)
# Set EMAIL_ENABLED=true to enable, false to disable
EMAIL_ENABLED=false
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USERNAME=your-email@gmail.com
EMAIL_SMTP_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com
EMAIL_TO=recipient@example.com
EMAIL_USE_TLS=true
